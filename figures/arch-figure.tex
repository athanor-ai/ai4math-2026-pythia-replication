% TikZ source for the pythia dispatcher architecture figure.
% Lives under figures/ so paper_lint skips it (TikZ uses ; as
% statement terminator, which the prose semicolon-density rule
% would otherwise flag).
\begin{tikzpicture}[
  node distance=0.45cm and 0.5cm,
  every node/.style={font=\footnotesize},
  box/.style={draw, rounded corners=2pt, align=center,
              inner sep=3pt, minimum height=0.55cm,
              text width=2.9cm},
  fall/.style={->, >=latex, thick},
  side/.style={draw, rounded corners=2pt, align=center,
               inner sep=3pt, minimum height=0.55cm,
               text width=2.9cm, fill=black!5},
]
\node[box] (goal) {goal};
\node[box, below=of goal] (s1)
  {stage 1: \texttt{aesop} \\ ruleset \texttt{Pythia} \\ ($@[\texttt{stat\_lemma}]$)};
\node[box, below=of s1] (s2)
  {stage 2: \texttt{simp;} \\ \texttt{omega; linarith;} \\ \texttt{positivity}};
\node[box, below=of s2] (s3) {closed (kernel-checked)};
\node[side, right=1.0cm of s2] (z3)
  {\texttt{z3\_check} \\ (separate tactic) \\ Z3 + \texttt{linarith}};
\draw[fall] (goal) -- (s1);
\draw[fall] (s1) -- node[right]{\scriptsize fail} (s2);
\draw[fall] (s2) -- (s3);
\draw[fall, dashed] (z3.west) -- ++(-0.3,0) |- (s3.east);
\end{tikzpicture}
